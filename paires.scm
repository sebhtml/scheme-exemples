; tout peut être défini avec une paire

; cons déclare une paire

(define foo (cons 1 2))

; car permet d'obtenir le premier élément d'une paire
(print (car foo))

(newline)

; cdr permet d'obtenir le deuxième élément d'une paire
(print (cdr foo))